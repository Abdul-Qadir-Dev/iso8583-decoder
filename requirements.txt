# Installs this project in editable mode, pulling dependency versions
# from pyproject.toml's [project.dependencies] -- that table is the
# single source of truth for versions, so there's nothing here to fall
# out of sync with it.
-e .
